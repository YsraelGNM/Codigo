function Sumar (a,b) {
    return a+b;
  }

  console.log(Sumar(10,5));

  let sumarflecha = (a,b)=>{
      return a+b;
  }

  console.log(sumarflecha(20,35));

  //aqui abajo solo si son asi las funciones (funciones con un parametro y una linea de codigo en el return)
  let cuadrado = nro => nro*nro

  console.log(cuadrado(7));

  