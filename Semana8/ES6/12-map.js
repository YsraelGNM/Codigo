let arreglo = [1,4,6,8,10,12,14];
//map retorna los elementos modificados a un nuevo arreglo.
let dobles = arreglo.map((elemento)=>{
    return elemento * 2;
});
// forma simplificada del map
let dobles = arreglo.map(elemento=>elemento*2);
